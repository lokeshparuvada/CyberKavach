// Citizen Fraud Shield -- runtime configuration
//
// Set this to wherever your mobile_api.py backend is actually reachable.
//
// - Local testing (browser + backend on the same machine):
//     window.API_BASE = "http://localhost:5003";
//
// - Real deployment / APK build (backend hosted somewhere with HTTPS):
//     window.API_BASE = "https://your-backend-domain.com";

window.API_BASE = "http://localhost:5003";